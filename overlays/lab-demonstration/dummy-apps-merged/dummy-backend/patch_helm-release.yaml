apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: dummy-backend
  namespace: dummy-backend
spec:
  chart:
    spec:
      version: 6.0.2
  values:
    image:
      tag: 0.1.4 # {"$imagepolicy": "dummy-apps:dummy-backend:tag"}
    ingress:
      enabled: true
      annotations:
        external-dns.alpha.kubernetes.io/hostname: dummy.quickanddirty.work
      rules:
        - host: dummy.quickanddirty.work
          paths:
            - pathType: Prefix
              path: "/api"
      tls:
        - hosts:
            - dummy.quickanddirty.work
          secretName: dummy-backend-tls
