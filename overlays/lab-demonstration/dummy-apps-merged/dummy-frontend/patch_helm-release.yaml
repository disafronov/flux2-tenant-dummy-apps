apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: dummy-frontend
  namespace: dummy-frontend
spec:
  chart:
    spec:
      version: 6.0.2
  values:
    image:
      tag: 0.1.60 # {"$imagepolicy": "dummy-apps:dummy-frontend:tag"}
    ingress:
      enabled: true
      annotations:
        external-dns.alpha.kubernetes.io/hostname: dummy.quickanddirty.work
      rules:
        - host: dummy.quickanddirty.work
          paths:
            - pathType: Prefix
              path: "/"
      tls:
        - hosts:
            - dummy.quickanddirty.work
          secretName: dummy-frontend-tls
