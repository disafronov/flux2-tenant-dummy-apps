apiVersion: source.toolkit.fluxcd.io/v1beta1
kind: GitRepository
metadata:
  name: dummy-backend
  namespace: dummy-backend
spec:
  url: ssh://git@github.com/DmitriySafronov/fleet-infra.git
  interval: 5m0s
  ref:
    branch: master
  secretRef:
    name: dummy-backend-git-auth
