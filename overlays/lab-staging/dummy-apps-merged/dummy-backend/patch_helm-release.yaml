apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: dummy-backend
  namespace: dummy-backend
spec:
  chart:
    spec:
      version: 10.5.1
  values:
    image:
      tag: 0.1.7-rc.1 # {"$imagepolicy": "dummy-apps:dummy-backend:tag"}
    ingress:
      enabled: true
      annotations:
        external-dns.alpha.kubernetes.io/hostname: dummy-staging.quickanddirty.work
      rules:
        - host: dummy-staging.quickanddirty.work
          paths:
            - pathType: Prefix
              path: "/api"
      tls:
        - hosts:
            - dummy-staging.quickanddirty.work
          secretName: dummy-backend-tls
