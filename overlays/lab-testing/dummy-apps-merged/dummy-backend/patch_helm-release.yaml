apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: dummy-backend
  namespace: dummy-backend
spec:
  chart:
    spec:
      version: 6.0.2
  values:
    image:
      tag: 0.1.0 # {"$imagepolicy": "dummy-apps-merged:dummy-backend:tag"}
    ingress:
      enabled: true
      annotations:
        external-dns.alpha.kubernetes.io/hostname: dummy-testing.quickanddirty.work
      rules:
        - host: dummy-testing.quickanddirty.work
          paths:
            - pathType: Prefix
              path: "/api"
      tls:
        - hosts:
            - dummy-testing.quickanddirty.work
          secretName: dummy-backend-tls
    autoscaling:
      enabled: true
