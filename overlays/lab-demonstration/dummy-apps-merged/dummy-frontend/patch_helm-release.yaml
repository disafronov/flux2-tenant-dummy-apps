apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: dummy-frontend
  namespace: dummy-frontend
spec:
  chart:
    spec:
      version: 10.5.1
  values:
    image:
      tag: 1.0.0 # {"$imagepolicy": "dummy-apps:dummy-frontend:tag"}
    ingress:
      enabled: true
      annotations:
        external-dns.alpha.kubernetes.io/hostname: dummy.quickanddirty.work
      rules:
        - host: dummy.quickanddirty.work
          paths:
            - pathType: Prefix
              path: "/"
      tls:
        - hosts:
            - dummy.quickanddirty.work
          secretName: dummy-frontend-tls
    autoscaling:
      enabled: true
